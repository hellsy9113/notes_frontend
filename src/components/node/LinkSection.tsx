/**
 * Placeholder for the link section.
 */
import type { LinkSectionData } from '../../types/node';

interface LinkSectionProps {
  data?: LinkSectionData;
}

export default function LinkSection({ data: _data }: LinkSectionProps) {
  // Currently hidden on the canvas, or returning null until implemented.
  return null;
}
